backfill_history:
  name: Backfill history
  description: >-
    Re-run the one-time deep import of halfhourly usage history (as far back
    as the Watercare API allows, roughly the last 3 years). Safe to run
    again; it only adds statistics for hours not already recorded.
